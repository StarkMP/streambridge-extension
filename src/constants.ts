import { Languages } from './types';

export const hostname = 'www.twitch.tv';
export const maxFollowedChannels = 5;
export const defaultLocale = Languages.English;
export const sidebarUpdateInterval = 60000;
